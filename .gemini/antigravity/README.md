# Gemini Configuration for Gamilit

## 🔗 INHERITANCE
This project inherits directly from the **Workspace V2** configuration.
It uses the **Dynamic Bootloader** pattern.

## 🚀 START HERE
**Execute the Bootloader Protocol**:
`../../../../.gemini/antigravity/BOOTLOADER_PROTOCOL.md`

## 🌍 CONTEXT
-   **Scope**: `projects/gamilit/`
-   **Documentation Map**: `orchestration/MAPA-DOCUMENTACION-GAMILIT.yml` (Use this instead of the root map)
-   **Inheritance**: This is a **STANDALONE** project.
    -   It has its own Inventories (`orchestration/inventarios/`).
    -   It has its own Directives (`orchestration/directivas/`).

> **NOTE on Subagents**: Gemini operates singly. Flatten any "Delegation" steps into sequential execution.
