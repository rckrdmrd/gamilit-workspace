DATABASE FORENSICS: ML COINS +1100 JUMP
========================================

PROBLEM:
  User ML Coins: 135 → 1235 (+1100)
  Header/Shop desync after coins update

DATABASE ANALYSIS RESULT:
  ✅ DDL layer is SAFE - No structural double-application mechanism
  ⚠️ Root cause is APPLICATION layer - Calling rank promotion/achievement function twice

RANK BONUSES (seed verified):
  Ajaw         → 0 coins
  Nacom        → 100 coins
  Ah K'in      → 250 coins
  Halach Uinic → 500 coins
  K'uk'ulkan   → 1000 coins

CRITICAL FUNCTIONS:
  promote_to_next_rank()
    - Award rank bonus: current_balance + ml_coins_bonus
    - Lookup bonus from maya_ranks (dynamic)
    - Insert into user_ranks history
    - Insert into ml_coins_transactions audit
    - ⚠️ NO IDEMPOTENCY: If called twice = double coins

  claim_achievement_reward()
    - Check rewards_claimed flag (IDEMPOTENT)
    - If already claimed: error, no coins
    - If not claimed: mark true + award coins
    - ✅ SAFE: Second call returns error

  award_ml_coins()
    - Apply rank multiplier (1.00x to 2.00x)
    - Insert transaction record
    - ✅ SAFE: Multiplier only, not source of duplication

TRIGGERS (12 total in gamification_system):
  user_stats:
    - AFTER UPDATE: trg_check_rank_promotion_on_xp_gain
      └─ Calls check_rank_promotion() ONCE per update
      └─ Calls promote_to_next_rank() if eligible
      └─ No recursion or doubling

  user_achievements:
    - AFTER INSERT/UPDATE: trg_achievement_unlocked
      └─ Creates notification only
      └─ Does NOT award coins (CORR-DUP-001 fix)
      └─ Requires explicit claim_achievement_reward() call

AUDIT TRAIL:
  ml_coins_transactions table: 70 records
  Columns: user_id, amount, balance_before, balance_after, transaction_type,
           description, reference_id, reference_type, multiplier, metadata
  ✅ Complete history available for investigation

ROOT CAUSE HYPOTHESIS (95% confidence):
  Backend service called promote_to_next_rank() or claim_achievement_reward()
  multiple times for same event:

  Possible triggers:
    1. Retry middleware + transient error (most likely)
    2. WebSocket event handler + HTTP handler both firing
    3. Race condition in async orchestration
    4. Message queue duplicate delivery
    5. Explicit double-call bug in RanksService/AchievementsService

STRUCTURAL VULNERABILITIES IN DDL:
  ⚠️ promote_to_next_rank() lacks idempotency token
  ⚠️ No UNIQUE constraint preventing duplicate rank awards
  ⚠️ Relies entirely on application to call once per event

SAFE MECHANISMS IN DDL:
  ✅ claim_achievement_reward() has rewards_claimed flag
  ✅ Row locks (FOR UPDATE) prevent race conditions
  ✅ Transaction logging is complete
  ✅ Trigger chain is sequential (no parallel execution)

EVIDENCE COLLECTION FOR PHASE 2:
  1. Backend request logs: timestamp near +1100 jump
  2. ml_coins_transactions: query for (user_id, same timestamp, same rank_type)
  3. RanksService: grep for duplicate promote calls
  4. WebSocket handlers: check for race with HTTP handlers
  5. Retry middleware: verify not re-executing XP updates

NEXT STEPS:
  Phase 2: Backend Analysis (2-3 Sonnet agents, 2-3 hours)
    - Trace exact call path in RanksService.promoteUserRank()
    - Trace exact call path in AchievementsService.claimReward()
    - Check WebSocket event handlers
    - Examine request logs for timestamps

  Phase 3: Data Remediation (if duplicate confirmed)
    - Identify exact duplicate transactions in audit table
    - Create reversal transaction
    - Update user_stats to correct balance
    - Log all changes in audit trail

---
Phase 1 Complete: ✅
Confidence: 95% (database layer fully analyzed)
Status: Ready for Phase 2 backend analysis
