===================================================================================
            FLUJO VISUAL: COMPARACIÓN init-database.sh vs reset-database.sh
===================================================================================

INIT-DATABASE.SH - FLUJO COMPLETO Y CORRECTO
─────────────────────────────────────────────────────────────────────────────────

    START
      │
      ├─ check_prerequisites()
      │   ├─ psql disponible? 
      │   ├─ openssl disponible?
      │   └─ Directorios DDL/SEEDS existen?
      │
      ├─ create_user_and_database()
      │   ├─ CREATE USER gamilit_user
      │   ├─ DROP DATABASE (si existe)
      │   └─ CREATE DATABASE gamilit_platform
      │
      ├─ execute_ddl()
      │   │
      │   ├─ [PASO 1] EJECUTA 00-prerequisites.sql ✓ CORRECTO
      │   │   ├─ CREATE SCHEMA (x10)        [Líneas 11-20]
      │   │   ├─ CREATE TYPE (ENUMs)        [Líneas 26-145]
      │   │   └─ CREATE FUNCTION            [Líneas 151-289]
      │   │
      │   ├─ [PASO 2] Crea schemas nuevamente   [REDUNDANTE]
      │   │   └─ CREATE SCHEMA IF NOT EXISTS (x9)
      │   │
      │   ├─ [PASO 3] Crea tablas por schema  [ORDEN ALFABÉTICO]
      │   │   ├─ auth/01-users.sql
      │   │   ├─ auth_management/01-tenants.sql
      │   │   ├─ auth_management/02-auth_attempts.sql
      │   │   ├─ auth_management/03-profiles.sql
      │   │   ├─ ... (40+ archivos más)
      │   │   │
      │   │   └─ Conteo: table_count, error_count
      │   │
      │   └─ [PASO 4] EJECUTA 99-post-ddl-permissions.sql ✓ CORRECTO
      │       ├─ GRANT USAGE ON SCHEMA
      │       ├─ GRANT PRIVILEGES ON TABLES
      │       ├─ GRANT PRIVILEGES ON SEQUENCES
      │       ├─ GRANT EXECUTE ON FUNCTIONS
      │       └─ ALTER DEFAULT PRIVILEGES
      │
      ├─ load_seeds()
      │   ├─ Orden de 32 seeds respetando FK ✓ CORRECTO
      │   ├─ Grupo 1: tenants, auth_providers
      │   ├─ Grupo 2: users
      │   ├─ Grupo 3: profiles
      │   ├─ Grupo 4: user_roles, user_preferences, etc.
      │   ├─ ... (7 grupos más)
      │   │
      │   └─ Detección de errores: grep -i "error"
      │
      ├─ validate_installation()
      │   ├─ COUNT schemas = 9?
      │   ├─ COUNT tablas
      │   ├─ COUNT usuarios
      │   └─ COUNT módulos
      │
      ├─ show_summary()
      │   ├─ Mostrar conexión
      │   ├─ Guardar credenciales
      │   └─ Ejecutar update-env-files.sh (opcional)
      │
      └─ END ✓


RESET-DATABASE.SH - FLUJO CON PROBLEMA CRÍTICO
─────────────────────────────────────────────────────────────────────────────────

    START
      │
      ├─ check_prerequisites()
      │   └─ [similar a init-database.sh]
      │
      ├─ drop_database()
      │   ├─ Verificar si DB existe
      │   └─ DROP DATABASE IF EXISTS
      │
      ├─ create_database()
      │   ├─ CREATE DATABASE gamilit_platform
      │   └─ GRANT PRIVILEGES
      │
      ├─ execute_ddl()
      │   │
      │   ├─ [PASO 1] NO EJECUTA 00-prerequisites.sql  ✗ CRÍTICO
      │   │   └─ Falta: ENUMs, funciones, esquemas
      │   │
      │   ├─ [PASO 2] Crea schemas
      │   │   └─ CREATE SCHEMA IF NOT EXISTS (x9)
      │   │
      │   ├─ [PASO 3] Intenta crear ENUMs desde carpeta ✗ FALLA
      │   │   ├─ if [ -d "$DDL_DIR/schemas/gamification_system/enums" ]
      │   │   └─ for enum_file in .../*.sql
      │   │       └─ PROBLEMA: Directorio no existe como se espera
      │   │           Solo tiene: maya_rank.sql
      │   │           Pero ENUMs están en 00-prerequisites.sql
      │   │
      │   ├─ [PASO 4] Crea tablas por schema  [ORDEN ALFABÉTICO]
      │   │   └─ POTENCIAL FALLO: Si tablas usan tipos/funciones de prereq
      │   │
      │   └─ [PASO 5] EJECUTA 99-post-ddl-permissions.sql ✓
      │       └─ [CORRECTAMENTE UBICADO]
      │
      ├─ load_seeds()
      │   ├─ Orden de 32 seeds respetando FK ✓ CORRECTO
      │   └─ Detección de errores: grep -i "error"
      │
      ├─ confirm_reset()
      │   └─ Pedir confirmación (yes/no)
      │
      └─ END ⚠ (potencialmente fallido)


RECREATE-DATABASE.SH - FLUJO DELEGADO
─────────────────────────────────────────────────────────────────────────────────

    START
      │
      ├─ check_prerequisites()
      │
      ├─ drop_database()
      │
      ├─ drop_user()
      │   ├─ DROP OWNED BY gamilit_user CASCADE
      │   └─ DROP USER IF EXISTS gamilit_user
      │
      ├─ reinitialize()
      │   │
      │   └─ bash init-database.sh --env $ENVIRONMENT
      │       │
      │       ├─ create_user_and_database()        ✓
      │       ├─ execute_ddl()                     ✓ (heredado de init)
      │       ├─ load_seeds()                      ✓
      │       ├─ validate_installation()           ✓
      │       └─ show_summary()                    ✓
      │
      ├─ confirm_deletion()
      │   ├─ Mostrar advertencia ROJA
      │   ├─ "DELETE ALL" confirmation
      │   └─ Contador 3-2-1 antes de ejecutar
      │
      └─ END ✓


===================================================================================
                        COMPARACIÓN LADO A LADO
===================================================================================

ETAPA                    init-database.sh    reset-database.sh    ESTADO
─────────────────────────────────────────────────────────────────────────────
00-prerequisites.sql     ✓ Línea 263         ✗ Falta             CRÍTICO
ENUMs                    ✓ En prereq         ✗ Intenta dir       CRÍTICO
Funciones                ✓ En prereq         ✗ No ejecuta        CRÍTICO
CREATE SCHEMA (x9)       ✓ Creadas           ✓ Creadas           OK
CREATE SCHEMA (x10 extra)✓ En prereq         ✓ En prereq         OK (redundante)
CREATE TABLE             ✓ Por carpeta       ✓ Por carpeta       OK
GRANT PRIVILEGES         ✓ Línea 339         ✓ Línea 298         OK
Order de SEEDS           ✓ Correcto          ✓ Correcto          OK
Error handling           ~ Oculto            ~ Oculto            MEJORAR


===================================================================================
                        DEPENDENCIA DE EJECUCIÓN
===================================================================================

00-prerequisites.sql  (DEBE SER PRIMERO)
├─ CREATE SCHEMA gamilit
├─ CREATE SCHEMA gamification_system
├─ CREATE SCHEMA auth
├─ CREATE SCHEMA auth_management
├─ CREATE SCHEMA system_configuration
├─ CREATE SCHEMA educational_content
├─ CREATE SCHEMA content_management
├─ CREATE SCHEMA social_features
├─ CREATE SCHEMA progress_tracking
├─ CREATE SCHEMA audit_logging
│
├─ CREATE TYPE gamilit_role
├─ CREATE TYPE user_status
├─ CREATE TYPE auth_provider
├─ CREATE TYPE maya_rank
├─ CREATE TYPE achievement_category
├─ ... (17 ENUMs más)
│
├─ CREATE FUNCTION gamilit.now_mexico()
├─ CREATE FUNCTION gamilit.update_updated_at_column()
├─ CREATE FUNCTION gamilit.get_current_user_role()
├─ CREATE FUNCTION gamilit.get_current_user_id()
├─ CREATE FUNCTION gamilit.get_current_tenant_id()
├─ CREATE FUNCTION gamilit.is_admin()
├─ CREATE FUNCTION gamilit.audit_profile_changes()
├─ CREATE FUNCTION gamilit.initialize_user_stats()
├─ CREATE FUNCTION gamilit.update_user_stats_on_exercise_complete()
├─ CREATE FUNCTION gamilit.update_classroom_member_count()
├─ CREATE FUNCTION gamification_system.update_missions_updated_at()
└─ CREATE FUNCTION gamification_system.update_notifications_updated_at()

↓↓↓ REQUIERE ↓↓↓

CREATE TABLE auth.users (NECESITA: ENUMs user_status, auth_provider)
CREATE TABLE auth_management.profiles (NECESITA: FUNCIÓN gamilit.now_mexico())
CREATE TABLE educational_content.exercises (NECESITA: ENUM exercise_type)
... (40+ tablas más, todas potencialmente dependientes)

↓↓↓ LUEGO ↓↓↓

99-post-ddl-permissions.sql (DEBE SER ÚLTIMO)
├─ GRANT USAGE ON SCHEMA
├─ GRANT PRIVILEGES ON TABLES
├─ GRANT PRIVILEGES ON SEQUENCES
├─ GRANT EXECUTE ON FUNCTIONS
└─ ALTER DEFAULT PRIVILEGES


===================================================================================
                            ESTADO POR SCRIPT
===================================================================================

init-database.sh:
  [✓] Prerequisitos primero
  [✓] ENUMs antes de tablas
  [✓] Funciones antes de triggers
  [✓] Seeds en orden correcto
  [✓] Permisos al final
  [~] Error handling (silencioso)
  [~] Schemas redundantes
  → ESTADO: 70% CORRECTO

reset-database.sh:
  [✗] NO ejecuta prerequisitos
  [✗] ENUMs desde directorio inexistente
  [✗] Funciones no se cargan
  [✓] Seeds en orden correcto
  [✓] Permisos al final
  [~] Error handling (silencioso)
  [~] Schemas creados (pero sin prereq)
  → ESTADO: 40% CORRECTO

recreate-database.sh:
  [✓] Delega a init-database.sh
  [✓] Elimina usuario completamente
  [✓] Confirmación doble
  → ESTADO: 70% CORRECTO (hereda de init)


===================================================================================
                        PROBLEMAS VISUALIZADOS
===================================================================================

PROBLEMA 1: reset-database.sh intenta create ENUMs desde carpeta inexistente

    DDL_DIR = /.../.../ddl/
    ├─ 00-prerequisites.sql        ← CONTIENE todos los ENUMs (líneas 26-145)
    ├─ 99-post-ddl-permissions.sql
    ├─ functions/
    ├─ migrations/
    ├─ views/
    └─ schemas/
       ├─ auth/
       ├─ gamification_system/
       │  ├─ enums/
       │  │  └─ maya_rank.sql      ← SÓLO ESTO (intenta buscar aquí)
       │  └─ tables/
       └─ ... (otros schemas sin enums/)
    
    Reset-database.sh INTENTA: for enum_file in schemas/gamification_system/enums/*.sql
    RESULTADO: FALLA porque estructura no es como espera
    
    SOLUCIÓN: Ejecutar 00-prerequisites.sql como hace init-database.sh


PROBLEMA 2: Inconsistencia entre scripts para DDL

    init-database.sh:
      1. Ejecuta 00-prerequisites.sql   ✓
      2. Crea esquemas              ✓
      3. Crea tablas                ✓
      4. Otorga permisos            ✓
    
    reset-database.sh:
      1. NO ejecuta 00-prerequisites.sql  ✗
      2. Intenta crear ENUMs manualmente  ✗
      3. Crea esquemas              ✓
      4. Crea tablas                ✓
      5. Otorga permisos            ✓
    
    RESULTADO: Scripts divergentes en flujo DDL
    SOLUCIÓN: Sincronizar ambos scripts


===================================================================================

FIN DE FLOWCHART

